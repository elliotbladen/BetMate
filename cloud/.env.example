# Server-side secrets only. Never expose the service-role key to a browser.
ODDS_API_KEY=
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Keep false until migration and dry-run checks pass.
ODDS_COLLECTION_LIVE_ENABLED=false
ODDS_WORKER_ID=betmate-market-worker-1

# Separate Railway cron service for official race-day sectionals and shadow tempo.
TEMPO_COLLECTION_LIVE_ENABLED=false
TEMPO_WORKER_ID=betmate-tempo-worker-1
